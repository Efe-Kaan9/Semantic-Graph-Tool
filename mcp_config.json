{
  "mcpServers": {
    "semantic-graph": {
      "command": "/path/to/your/venv/bin/python",
      "args": [
        "/path/to/semantic-graph/server.py"
      ],
      "env": {},
      "description": "Semantic Code Knowledge Graph MCP Server. IMPORTANT: Every tool call requires target_directory set to the user's project workspace absolute path."
    }
  }
}